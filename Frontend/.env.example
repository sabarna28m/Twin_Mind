# ──────────────────────────────────────────────────────────────
# TwinMind Frontend — Environment Variables
# Copy this to .env and fill real values. NEVER commit .env.
# ──────────────────────────────────────────────────────────────

# Backend API base URL — must end with /api/v1
# Local:      http://localhost:8000/api/v1
# Production: https://twinmind-api.onrender.com/api/v1
VITE_API_URL=http://localhost:8000/api/v1
